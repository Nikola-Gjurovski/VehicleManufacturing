@model Domain.DTO.UsersDto
@*
    For more information on enabling MVC for empty projects, visit https://go.microsoft.com/fwlink/?LinkID=397860
*@
@{
    var successMessage = TempData["SuccessMessage"] as string;
}
@* <div class="row">
    <div class="col-md-4">
      
        <form method="post" asp-action="SetAdmin">
            
            <div class="form-group">
                <label asp-for="Id" class="control-label"></label>
                @Html.DropDownListFor(x => Model.Id, new SelectList(Model.users, "Id", "Email"), new { @class = "form-control" })
                <span asp-validation-for="Id" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div> *@
@if (!string.IsNullOrEmpty(successMessage))
{
    <div class="alert alert-success">
        @successMessage
    </div>
}
<table class="table table-bordered table-striped" id="tblUsers">
    <thead>
        <tr>
            <th>First Name</th>
            <th>Last Name</th>
            <th>Email Address</th>
            <th>Address</th>
        </tr>
    </thead>
  @*   <tbody>
        @foreach (var item in Model.users)
        {
            <tr>
                <td>@Html.DisplayFor(modelItem => item.FirstName)</td>
                <td>@Html.DisplayFor(modelItem => item.LastName)</td>
                <td>@Html.DisplayFor(modelItem => item.Email)</td>
                <td>@Html.DisplayFor(modelItem => item.Adress)</td>
            </tr>
        }
    </tbody> *@
</table>
<div>
    <a asp-action="Index">Go back</a>
</div>
<div>
    <a asp-action="Index">Go back</a>
</div>
@section Scripts {
    <script src="https://cdn.datatables.net/1.10.21/js/jquery.dataTables.min.js"></script>
    <link rel="stylesheet" href="https://cdn.datatables.net/1.10.21/css/jquery.dataTables.min.css" />
    <script src="~/js/Users.js"></script>
}